# Deep Beam Shear Capacity Prediction System

A Streamlit-based intelligent prediction system for reinforced concrete deep beams.

## Functions
- Solid deep beam prediction
- Deep beam with web openings prediction

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py